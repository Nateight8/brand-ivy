import Connect from "@/components/connect/Connect";
import React from "react";

type Props = {};

function Home({}: Props) {
  return (
    <main className="px-4">
      <Connect />
    </main>
  );
}

export default Home;
